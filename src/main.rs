//Hi Rishi, I think this would be a better way to boot up the OS in Rust. Let me know what you think.

fn main() {
    loop { //loop
        println!("Hello, Mom!"); //print statement
    } //loop ends
}
